#include <stdio.h>
#include <stdlib.h>
#include <time.>

/**
 * main - Entry point
 * Description: prints all the numbers thatmake up the 
 * decimal number system and then prints newline.
 * Return: 0 (Successful)
 */
int main(void)
{
	int num;
	for (num = 0; num < 10; num++)
	{
		putchar('0; + num);
	}
	
	puchar ('\n');

	return (0);
}
